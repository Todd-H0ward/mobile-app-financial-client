/**
 * Minimum touch target in density-independent pixels — requirement 3.6.
 * Kept in a Platform-free module so pure helpers and vitest can import it
 * without loading React Native.
 */
export const HIT_SLOP_SIZE = 48;
