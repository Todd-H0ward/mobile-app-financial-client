/**
 * Shared arcade scenes — `GameShell`, sitting chrome, debrief.
 *
 * Per-game UI lives behind its own entry so one game does not pull another:
 *
 * - `@/widgets/minigame/puzzle` — jigsaw board, tray, ghost
 */

export {};
