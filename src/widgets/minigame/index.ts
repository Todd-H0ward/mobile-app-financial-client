/**
 * Shared arcade scenes — per-game UI lives behind its own entry so one game
 * does not pull another:
 *
 * - `@/widgets/minigame/console` — handheld chrome, D-pad, face buttons
 * - `@/widgets/minigame/finance` — market / weekly GameShell
 * - `@/widgets/minigame/playkit` — Overseer CRT trials
 * - `@/widgets/minigame/puzzle` — jigsaw board, tray, ghost
 * - `@/widgets/minigame/snake` — swipe snake field
 * - `@/widgets/minigame/spacewar` — ship shoot-out
 */

export {};
